# Batch Folder Workflow

```sh
./byom-video batch media/inbox --preset metadata
./byom-video batch media/inbox --preset shorts --recursive --limit 5
./byom-video batches
./byom-video inspect-batch <batch_id>
```
