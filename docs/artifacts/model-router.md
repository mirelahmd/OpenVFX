# Model Router Artifacts

Current router-side artifacts:

- `model_requests.dryrun.json`

These artifacts are local execution contracts only. They do not imply that any cloud or remote provider was called.
