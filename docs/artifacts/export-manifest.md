# `export_manifest.json`

Planned-versus-rendered export ledger for a run.

Path:

```text
.byom-video/runs/<run_id>/export_manifest.json
```

Produced by:

```sh
./byom-video export-manifest <run_id>
```
